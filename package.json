{
  "name": "teamhf-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && yarn install",
    "build": "cd frontend && CI=true yarn build",
    "start": "cd frontend && yarn start"
  }
}
